fragment nft on NFTEntity {
  id
  name
  metadata
  issuer
  currentOwner
  blockNumber
}

query nfts($first: Int! $offset: Int) {
  nFTEntities(first: $first offset: $offset orderBy: BLOCK_NUMBER_ASC) {
    totalCount
    nodes {
      ...nft
    }
  }
}
