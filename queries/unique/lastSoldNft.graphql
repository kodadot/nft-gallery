
query lastSoldNft {
  events(limit: 10, where: { interaction_eq: BUY }, orderBy: blockNumber_DESC) {
    meta
    timestamp
    nft {
      id
      name
      issuer
      currentOwner
      meta {
        id
        image
      }
    }
  }
}
