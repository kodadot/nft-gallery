
query newestListNft {
  events(limit: 10, where: { interaction_eq: LIST }, orderBy: blockNumber_DESC) {
    meta
    timestamp
    nft {
      id
      name
      issuer
      meta {
        image
      }
    }
  }
}
