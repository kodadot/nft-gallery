query seriesInsightList(
  $dateRange: String
  $limit: Float
  $offset: String
  $orderBy: String
  $orderDirection: String
) {
  seriesInsightTable(
    dateRange: $dateRange
    limit: $limit
    offset: $offset
    orderBy: $orderBy
    orderDirection: $orderDirection
  ) {
    averagePrice
    buys
    highestSale
    floorPrice
    id
    image
    metadata
    name
    sold
    total
    unique
    uniqueCollectors
    volume
  }
}
