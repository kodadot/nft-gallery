query collectorList($limit: Int!, $offset: Int!) {
  collectionEntities: collectors(limit: $limit, offset: $offset) {
    id
    unique
    total
    max
    averagePrice: average
    count: collections
    uniqueCollectors
    volume
  }
}
