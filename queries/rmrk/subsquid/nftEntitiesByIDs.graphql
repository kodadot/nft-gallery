query nftEntitiesByIDs($ids: [ID!]!) {
  nftEntities(where: { id_in: $ids, price_gt: "0" }, limit: 10) {
    id
    name
    currentOwner
    issuer
    meta {
      id
      image
    }
    price
    updatedAt
  }
}
