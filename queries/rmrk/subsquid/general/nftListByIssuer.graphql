#import "../../../fragments/nftDetails.graphql"
#import "../../../fragments/rubickNft.graphql"
query nftListByIssuer(
  $account: String!
  $limit: Int!
  $offset: Int
  $search: [NFTEntityWhereInput!]
  $orderBy: NFTEntityOrderByInput = blockNumber_DESC
) {
  nftEntities(
    where: {
      issuer_eq: $account
      name_not_contains: "%Kanaria%"
      burned_eq: false
      AND: $search
    }
    orderBy: [$orderBy, blockNumber_DESC]
    limit: $limit
    offset: $offset
  ) {
    ...rubickNft
    ...nftDetails
  }
  nftEntitiesConnection(
    where: {
      issuer_eq: $account
      name_not_contains: "%Kanaria%"
      burned_eq: false
      AND: $search
    }
    orderBy: blockNumber_DESC
  ) {
    totalCount
  }
}
