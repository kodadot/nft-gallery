query collectionSeriesList($limit: Float) {
  collectionEntities: seriesInsightTable(limit: $limit) {
    id
    floorPrice
    averagePrice
    image
    metadata
    sold
    name
    total
    totalBuys: buys
    unique
    uniqueCollectors
    volume
  }
}
