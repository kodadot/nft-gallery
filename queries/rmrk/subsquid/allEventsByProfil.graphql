query allEventsByProfil($id: String, $search: [EventWhereInput!]) {
  events(where: {caller_eq : $id, AND: $search }) {
    meta
    interaction
    blockNumber
    timestamp
    caller
    id
    nft {
      id
      name
      collection {
        id
        name
      }
    }
  }
}