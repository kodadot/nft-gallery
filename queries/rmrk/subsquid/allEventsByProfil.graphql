query allEventsByProfil($id: String, $and: [EventWhereInput!]) {
  events(where: {caller_eq : $id, AND: $and }) {
    meta
    interaction
    blockNumber
    timestamp
    caller
    id
    nft {
      id
      name
      collection {
        id
        name
      }
    }
  }
}