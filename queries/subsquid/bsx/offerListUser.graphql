query offerListUser($id: String!) {
  offers(orderBy: createdAt_DESC, where: { caller_eq: $id }) {
    blockNumber
    caller
    createdAt
    expiration
    id
    price
    status
    updatedAt
    nft {
      name
      id
    }
  }
}
