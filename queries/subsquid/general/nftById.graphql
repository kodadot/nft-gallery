#import "../../fragments/nft.graphql"
#import "../../fragments/nftDetails.graphql"

query nftById($id: ID!) {
  nftEntity: nftEntityById(id: $id) {
    ...nft
    ...nftDetails
    collection {
      name
    }
    events {
      interaction
      timestamp
      meta
    }
  }
}
