query collectionByIdMinimalWithRoyalty($id: String!) {
  collectionEntityById(id: $id) {
    id
    issuer
    royalty
    recipient
    meta {
      animationUrl
      description
      id
      image
      name
      type
    }
    nftCount
    max
    metadata
    name
    currentOwner
    createdAt
  }
}
