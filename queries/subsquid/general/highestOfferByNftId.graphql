query highestOfferByNftId($id: String!) {
    offers(where: {status_eq: ACTIVE, desired: {id_eq: $id}}, orderBy: price_DESC) {
    expiration
    status
    price
    id
    caller
    desired {
      name
      price
      id
    }
  }
}