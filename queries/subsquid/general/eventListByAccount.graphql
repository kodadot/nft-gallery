query eventListByAccount($account: String!) {
  events(where: { caller_eq: $account }, orderBy: timestamp_DESC) {
    id
    interaction
    timestamp
    caller
    nft {
      name
      id
      metadata
      price
      meta {
        id
        image
      }
      collection {
        id
      }
    }
  }
}
