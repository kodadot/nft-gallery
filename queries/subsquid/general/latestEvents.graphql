query latestEvents(
  $limit: Int!
  $orderBy: [EventOrderByInput!]
  $where: EventWhereInput
) {
  events(limit: $limit, orderBy: $orderBy, where: $where) {
    nft {
      id
      name
      price
      sn
      currentOwner
      issuer
      meta {
        id
        name
        image
        animationUrl
      }
      collection {
        id
        name
      }
    }
    timestamp
  }
}
