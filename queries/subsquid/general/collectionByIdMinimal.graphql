query collectionByIdMinimal($id: String!) {
  collectionEntityById(id: $id) {
    id
    issuer
    meta {
      animationUrl
      description
      id
      image
      banner
      name
      type
    }
    nftCount
    max
    metadata
    name
    currentOwner
    createdAt
  }
}
