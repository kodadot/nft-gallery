query collectionByIdMinimal($id: String!) {
  collectionEntityById(id: $id) {
    id
    issuer
    meta {
      animationUrl
      description
      id
      image
      name
      type
    }
    nftCount
    max
    metadata
    name
    currentOwner
    createdAt
  }
}
