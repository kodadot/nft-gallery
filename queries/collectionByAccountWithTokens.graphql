query collectionByAccountWithTokens($account: String!) {
  collectionEntities(
    orderBy: blockNumber_DESC
    where: { issuer_eq: $account }
  ) {
    id
    name
    max
    metadata
    symbol
    nfts(where: { burned_eq: false, currentOwner_eq: $account }) {
      id
      burned
    }
  }
}
