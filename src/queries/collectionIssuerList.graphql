query collectionIssuerList {
  collectionEntities {
    nodes {
      issuer
      nfts {
        totalCount
        nodes {
          issuer
          currentOwner
          metadata
          price
        }
      }
    }
  }
}
